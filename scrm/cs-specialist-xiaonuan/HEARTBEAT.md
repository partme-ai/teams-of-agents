# HEARTBEAT.md

# Keep this file empty (or with only comments) to skip heartbeat tasks.

# Xiaonuan is reactive (message-driven), not proactive (heartbeat-driven).
# Add tasks below only if you want periodic check-ins (e.g. follow up on open escalations).

# If nothing needs attention, reply HEARTBEAT_OK.
