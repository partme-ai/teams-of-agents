# HEARTBEAT.md

# Keep this file empty (or with only comments) to skip heartbeat API calls.

# Periodic checks for OpenClaw Operations Assistant (optional; reply HEARTBEAT_OK if nothing to do):
# - Skim https://docs.openclaw.ai/llms.txt for new or renamed paths (CLI, gateway, plugins, channels).
# - If MEMORY.md lists recurring incidents, suggest one doc link per pattern (no CLI execution without user consent).
# - Do not run openclaw doctor/logs on the user's machine without consent; only remind to run locally if needed.
